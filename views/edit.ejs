<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data View and Edit</title>
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #0b0a0a;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .form-container {
            background-color: #292929;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 15px;
            padding: 30px;
            width: 450px;
            color: #fff;
            padding: 50px;
        }

        .form-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-title {
            font-size: 32px;
            color: #f0f0f0;
            margin: 0;
        }

        .form-description {
            font-size: 16px;
            color: #b0b0b0;
        }

        .form-field {
            margin-bottom: 20px;
        }

        .form-label {
            font-size: 18px;
            color: #f0f0f0;
            margin-bottom: 5px;
        }

        .form-input {
            width: 100%;
            padding: 12px;
            border: 2px solid #666;
            border-radius: 8px;
            font-size: 18px;
            background-color: #1a1a1a;
            color: #f0f0f0;
        }

        .form-input[type="file"] {
            padding: 8px;
            font-size: 16px;
        }

        .form-button {
            background-color: #ff5733;
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 12px 20px;
            font-size: 18px;
            cursor: pointer;
        }

        .form-button:hover {
            background-color: #ff4318;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #ff5733;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #ddd;
        }
    </style>
    
</head>
<body>
    <div class="form-container">
        <h1>Data View and Edit</h1>
        <form method="POST" action="/updateDoc" enctype="multipart/form-data">
            <input type="hidden" name="id" value="<%= editdoc.id %>">
            <div class="form-field">
                <label class="form-label" for="txt">Name</label>
                <input class="form-input" type="text" name="txt" id="txt" placeholder="Enter your name" value="<%= editdoc.text %>" required>
            </div>
            <div class="form-field">
                <label class="form-label" for="email">Email Id</label>
                <input class="form-input" type="email" name="email" id="email" placeholder="Enter your email" value="<%= editdoc.email %>" required>
            </div>
            <div class="form-field">
                <label class="form-label" for="number">Phone Number</label>
                <input class="form-input" type="number" name="number" id="number" placeholder="Enter your phone number" value="<%= editdoc.number %>">
            </div>
            <div class="form-field">
                <label class="form-label" for="fileupload">Upload Image</label>
                <input class="form-input" type="file" name="fileupload" value="<%= editdoc.path %>">   
            </div>
            <button class="form-button" type="submit">Update</button>
        </form> 
    </div>
</body>
</html>
